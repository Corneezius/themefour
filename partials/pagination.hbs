<nav class="pagination" role="navigation">
    {{#if prev}}
        <a class="newer-posts" href="{{page_url prev}}"><i class="arrow_carrot-left"></i> Newer posts</a>
    {{/if}}
    {{!-- <span class="page-number">{{page}} of {{pages}}</span> --}}
    {{#if next}}
        <a class="older-posts" href="{{page_url next}}">Older posts <i class="arrow_carrot-right"></i> </a>
    {{/if}}
</nav>